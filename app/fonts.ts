import { Rubik, Libre_Franklin, DM_Sans } from "next/font/google";

export const rubik = Rubik({
    subsets: ['latin'],
    display: 'swap',
    style: 'normal',
});

export const libreFranklin = Libre_Franklin({
    subsets: ['latin'],
    display: 'swap',
    style: 'normal',
});

export const dmSans = DM_Sans({
    weight: "400",
    subsets: ['latin'],
    display: 'swap',
    style: 'normal',
});
